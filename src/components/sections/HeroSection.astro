---
// Hero Section Component - MabelGreen style split layout
---

<section class="hero-split relative min-h-screen overflow-hidden">
  <!-- Background color -->
  <div class="absolute inset-0 bg-warmth-100"></div>
  
  <!-- Left Image -->
  <div class="absolute left-0 top-[8%] bottom-[8%] w-[42%] z-10 opacity-0 animate-fade-in-up animate-delay-100">
    <div class="relative h-full ml-[8%] shadow-2xl overflow-hidden">
      <img 
        src="/images/czarkowice3.jpg" 
        alt="Klimatyczne wnętrze - bilard i wino"
        class="w-full h-full object-cover"
      />
    </div>
  </div>

  <!-- Right Image -->
  <div class="absolute right-0 top-0 bottom-0 w-[55%] z-0 opacity-0 animate-fade-in-up animate-delay-200">
    <img 
      src="/images/czarkowice1.jpg" 
      alt="Agroturystyka Czarkowice - widok na ośrodek"
      class="w-full h-full object-cover"
    />
  </div>

  <!-- Center Arch Overlay -->
  <div class="absolute inset-0 flex items-center justify-center z-20 pointer-events-none">
    <div class="hero-arch bg-white pointer-events-auto opacity-0 animate-fade-in-up animate-delay-300">
      <div class="arch-content text-center px-8 py-12 md:px-12 md:py-16">
        <!-- Logo -->
        <div class="mb-6 opacity-0 animate-fade-in-up animate-delay-400">
          <img 
            src="/images/logo.png" 
            alt="Czarkowice Logo" 
            class="h-16 md:h-20 mx-auto logo-dark"
          />
        </div>

        <!-- Main Heading -->
        <h1 class="font-serif text-3xl md:text-4xl lg:text-5xl text-forest-900 mb-6 leading-tight opacity-0 animate-fade-in-up animate-delay-400">
          <span class="block">ODPOCZYWAJ.</span>
          <span class="block">SMAKUJ.</span>
          <span class="block">CIESZ SIĘ.</span>
        </h1>

        <!-- Description -->
        <p class="text-warmth-600 text-base md:text-lg max-w-xs mx-auto mb-8 font-light leading-relaxed opacity-0 animate-fade-in-up animate-delay-500">
          Komfortowy wypoczynek w sercu polskiej wsi – idealne miejsce dla par i rodzin.
        </p>

        <!-- CTA Button -->
        <div class="opacity-0 animate-fade-in-up animate-delay-500">
          <a 
            href="#kontakt" 
            class="inline-block px-10 py-4 border-2 border-warmth-500 text-warmth-600 font-medium text-sm tracking-widest uppercase hover:bg-warmth-500 hover:text-white transition-all duration-300"
          >
            Zarezerwuj
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-split {
    min-height: 100vh;
    min-height: 100dvh;
  }

  .hero-arch {
    width: 340px;
    padding-bottom: 40px;
    border-radius: 170px 170px 0 0;
    box-shadow: 0 25px 80px -20px rgba(0, 0, 0, 0.15);
  }

  .arch-content {
    padding-top: 3rem;
  }

  .logo-dark {
    filter: invert(25%) sepia(10%) saturate(600%) hue-rotate(70deg) brightness(95%) contrast(90%);
  }

  @media (min-width: 768px) {
    .hero-arch {
      width: 420px;
      border-radius: 210px 210px 0 0;
    }
  }

  @media (min-width: 1024px) {
    .hero-arch {
      width: 480px;
      border-radius: 240px 240px 0 0;
    }

    .arch-content {
      padding-top: 4rem;
    }
  }

  @media (max-width: 767px) {
    .hero-split .absolute.left-0 {
      display: none;
    }

    .hero-split .absolute.right-0 {
      width: 100%;
      left: 0;
    }

    .hero-split .absolute.right-0::after {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(255, 255, 255, 0.3);
    }

    .hero-arch {
      width: 90%;
      max-width: 340px;
    }
  }
</style>
